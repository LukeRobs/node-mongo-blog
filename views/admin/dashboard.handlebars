<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Admin Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    .sidebar {
      height: 100vh;
      position: fixed;
      top: 0;
      left: 0;
      padding-top: 60px;
      background-color: #343a40;
      color: #fff;
      width: 220px;
    }
    .sidebar a {
      color: #fff;
      text-decoration: none;
      display: block;
      padding: 10px;
    }
    .sidebar a:hover {
      background-color: #495057;
    }
    .content {
      margin-left: 220px;
      padding: 20px;
    }
    .row {
    justify-content: center;
    }
    .card-body {
    text-align: center;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/admin">Admin Dashboard</a>
      <div class="d-flex">
        <span class="navbar-text me-3">Olá,{{usuario}}</span>
      </div>
    </div>
  </nav>

  <!-- Sidebar -->
  <div class="sidebar">
    <a href="/admin">Início</a>
    <a href="/admin/categorias">Categorias</a>
    <a href="/admin/postagens">Postagens</a>
    <a href="/usuarios/logout">Sair</a>
  </div>

    <!-- Conteúdo principal -->
    <main class="container my-4" style="max-width: 960px;">
        <h1 class="text-center">Dashboard</h1>
        <p class="text-center">Bem-vindo ao painel de administração.</p>

        <div class="row justify-content-center mt-4">
            <div class="col-md-4 mb-3">
                <div class="card text-white bg-primary text-center">
                <div class="card-body">
                <h5 class="card-title">Total de Categorias:</h5>
                <p class="card-text fs-4">{{totalCategorias}}</p>
                </div>
                </div>
            </div>
            <div class="col-md-4 mb-3">
            <div class="card text-white bg-success text-center">
                <div class="card-body">
                <h5 class="card-title">Total de Postagens:</h5>
                <p class="card-text fs-4">{{totalPostagens}}</p>
                </div>
            </div>
            </div>
            <div class="col-md-4 mb-3">
            <div class="card text-white bg-warning text-center">
                <div class="card-body">
                <h5 class="card-title">Total de Usuários:</h5>
                <p class="card-text fs-4">{{totalUsuarios}}</p>
                </div>
            </div>
            </div>
        </div>

    <h2 class="text-center mt-4">Resumo</h2>
    <p class="text-center">Gerencie facilmente suas categorias, postagens e usuários utilizando o menu à esquerda.</p>

    <div class="row justify-content-center mt-4">
        <div class="col-md-4 mb-3">
        <div class="card text-white bg-info text-center">
            <div class="card-body">
            <h5 class="card-title">Categorias criadas hoje</h5>
            <p class="card-text fs-4">{{categoriasHoje}}</p>
            </div>
        </div>
        </div>
        <div class="col-md-4 mb-3">
        <div class="card text-white bg-secondary text-center">
            <div class="card-body">
            <h5 class="card-title">Posts (últimos 7 dias)</h5>
            <p class="card-text fs-4">{{postsSemana}}</p>
            </div>
        </div>
        </div>
        <div class="col-md-4 mb-3">
        <div class="card text-white bg-dark text-center">
            <div class="card-body">
            <h5 class="card-title">Novos usuários (último mês)</h5>
            <p class="card-text fs-4">{{usuariosMes}}</p>
            </div>
        </div>
        </div>
    </div>
    </main>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
