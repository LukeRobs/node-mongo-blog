<div class="bg-light p-5 rounded-lg m-3">
  <h1 class="display-4">Bem vindo!</h1>
  <p class="lead">esse é um Simples Blog, desenvolvido em NodeJS + express + mongoDB.</p>
  <hr class="my-4">
  <p>Aqui você pode ler sobre as novidades do mundo da tecnologia</p>
  {{#if user}}

  {{else}}
  <a class="btn btn-primary btn-lg" href="/usuarios/cadastro" role="button">Crie sua conta</a>
  {{/if}}
</div>
<hr>

<h2>Postagens Recentes: </h2>

{{#each postagens}}
<div class="card">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia Mais</button></a>
        <br> <hr>
        <small>Categoria: {{categoria.nome}}</small> <br>
       
    </div>
</div>

{{else}}
    <h5>Nenhuma Postagem!!</h5>
{{/each}}